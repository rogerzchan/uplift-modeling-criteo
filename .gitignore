# Data (large, downloaded separately)
data/*
!data/.gitkeep

# Artifacts — the pickled models are large and machine-specific.
# results.csv and *.png figures ARE checked in so the README can pull them.
artifacts/models/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# uv
# (uv.lock IS checked in; .python-version IS checked in)

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Per-config sweep artifacts — large binary parquets. Keep only aggregate results.csv.
artifacts/experiments/*/

# Claude Code local settings — should not be committed.
.claude/
CLAUDE.md
